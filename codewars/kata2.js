/*function songDecoder(song){
    console.log('dupa');
 let title = song.toArray();
 for (i = 0; i <song.length ; i++){
  if (song[i] === "W" && song[i+1] === "U" && song[i+2] === "B" )
   i += 3;
  else 
  title.push(song[i]);
 };
  return title.toString();  
}
*/
function songDecoder(song){
    console.log("dupa");
};

